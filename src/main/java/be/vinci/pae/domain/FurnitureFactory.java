package be.vinci.pae.domain;

public interface FurnitureFactory {

  /**
   * This method create an empty UserImpl and returns it.
   * 
   * @return empty furnitureDTO.
   */
  FurnitureDTO getInstance();
}

package be.vinci.pae.domain;

public interface AddressFactory {

  /**
   * This method create an empty AddressImpl and returns it.
   * 
   * @return empty AddressDTO.
   */
  AddressDTO getInstance();
}
